# Scraping (free, no paid API) — needs a burner X account's cookies
twscrape>=0.13.0

# Local, free embeddings for retrieval (no API key)
sentence-transformers>=2.6.0
numpy>=1.24.0

# Free-tier hosted LLMs
groq>=0.11.0                 # primary  (free tier)
google-generativeai>=0.7.0  # fallback (free tier)

# Web chat UI (free to host on Hugging Face Spaces)
gradio>=4.44.0

# Config
python-dotenv>=1.0.0
