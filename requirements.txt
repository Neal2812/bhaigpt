# Core runtime for the HOSTED chatbot. Streamlit Community Cloud installs this
# file. Kept lean (no torch) so free deploys build fast and reliably — the
# hosted app runs in persona / few-shot mode. For the full local pipeline
# (scraping + embeddings + Gradio), use requirements-local.txt instead.
streamlit>=1.36.0
groq>=0.11.0                 # primary LLM  (free tier)
google-generativeai>=0.7.0  # fallback LLM (free tier)
numpy>=1.24.0
python-dotenv>=1.0.0
